<!DOCTYPE html>
<html>
<head>
  <title><%= username %>'s Profile</title>
  <link rel="stylesheet" href="/styles.css" />
</head>
<body>
  <div class="profile-container">
    <h2>@<%= username %> - <%= title %></h2>
    <p>Total Upvotes Received: <%= upvotes %></p>

    <h3>Posts</h3>
    <ul>
      <% posts.forEach(post => { %>
        <li><%= post.content %> - <%= new Date(post.createdAt).toLocaleString() %></li>
      <% }) %>
    </ul>
  </div>
</body>
</html>
